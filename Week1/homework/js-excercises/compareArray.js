'use strict'
const albums=['Thriller', 'Bad', 'Dangerous', 'History'];
const songs = ['Billie Jean', 'Beat It', 'Bad', 'Smooth Criminal', 'Man in the Mirror', 'Dangerous', 'Scream'];
console.log(`Albums has ${albums.length} items`);
console.log(`Songs has ${songs.length} items`);
albums.length ==songs.length ? console.log("They are the same!"): console.log("Two different sizes.");