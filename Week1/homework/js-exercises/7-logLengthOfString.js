'use strict';

const mySentence = 'national geographic';

console.log(mySentence.length); // the console will print how many letters are written as anumber

for (var i = 0; i<mySentence.length; i++) // This way the console will print the letters individually
{
    console.log(mySentence[i]);
};