"use strict";
//drclare variable
let mySentence="Programming is so interesting!";

//printing the length of my sentence use length method
console.log(mySentence.length);

console.log(`the length of my sentence is ${mySentence.length}`);
