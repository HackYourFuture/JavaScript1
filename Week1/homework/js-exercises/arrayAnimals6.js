"use strict";

let animalsArray = []; //Declare variable and assign to it an empty array. 

console.log(animalsArray);

animalsArray = ['Dog','Cat','Bird'];

console.log(animalsArray[1]); //the second array.

animalsArray.unshift("Piglet"); // Add a statement the beginning of the array

console.log(animalsArray[1]); //the new second array.
