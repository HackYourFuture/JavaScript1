'use strict';
const x = [1,2,3,4];

const y = ['q','w','e','r','t','y','u'];

console.log('The length of the array x is ' + x.length + " and the length of the array y is " + y.length);

if (x.length === y.lenght) {
    console.log("They are the same!");
}
    else {console.log("Two different sizes");}
