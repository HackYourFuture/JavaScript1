'use strict';

{
  // let mySentence = 'Programming is so interesting!';
  // const is used instead of let keyword. Because it's fixed variable.

  const mySentence = 'Programming is so interesting!';
  console.log(mySentence.length);
}
