// (1) declare a variable with string
let mySentence = "Programming is so interesting";

//  (2) as per google (mySentence.length)

// (3) log the the length of the string
console.log(mySentence.length);
//expected output:29
