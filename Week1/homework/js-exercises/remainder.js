'use script';{
    var x = 7;
    x = x % 3;
    console.log(x); // x is equal to 7. And if we execute x modula 3, we get 1 as the conclusion.
    var y = 21;
    y = y % 4;
    console.log(y);// y is equal to 21. And if we execute y modula 4, we get 1 as the conclusion.
    var z = 13;
    z = z % 2;
    console.log(z);// z is equal to 13. And if we execute z modula 3, we get 1 as the conclusion.
}