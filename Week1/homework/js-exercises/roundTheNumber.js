// (1) declare a variable z=7.5
let z = 7.25;

// (2) log z
console.log(z);
// expected output:7.25

// (3) declare a variable (a)
let a = Math.round(z);
//a = math.round(z);

// (4) log a
console.log(a);
// expected output:7

// (5) compare the two variables
let y = Math.max(a, z);

// (6) log the higest
console.log(y);
