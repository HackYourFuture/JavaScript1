'use strict';
const firstArray = ['html', 'css', 'sass', 'Bootstrap'];
const secondArray = ['Javascript', 'reactjs', 'nodejs', 'Vuejs', 'jquery', 'github', 'MySQL'];

console.log(`The langth of the first array is: ${firstArray.length}`);

console.log(`The langth of the second array is: ${secondArray.length}`);

if (firstArray.length == secondArray) {
    console.log('The are the same!');
} else {
    console.log('Two diffrent size');
}