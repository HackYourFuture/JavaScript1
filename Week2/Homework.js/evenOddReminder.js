'use strict';

for (let i = 0; i < 21; i++) {
  if (i % 2 == 1) {
    console.log('The number ' + i + ' is odd!'); //If remainder is 1 it is an odd number
  } else {
    console.log('The number ' + i + ' is even!'); // If there is no remainder the number is even
  }
}
