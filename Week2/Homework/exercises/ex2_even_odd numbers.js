"use strict";

let num = 20;
for (let i = 0; i <= num; i++) {
  if (i % 2 == 0) {
    console.log("The number %s is even!", i);
  } else {
    console.log("The number %s is odd!", i);
  }
}
