let bar = 42;
console.log(typeof typeof bar); /* It returns string because 
the type of bar is "number" and this is a string. So type of "number"
is being string*/

let typeOfBar = typeof bar;
console.log(typeOfBar);