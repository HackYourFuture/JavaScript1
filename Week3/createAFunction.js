// 1. Create a function that takes 3 arguments and returns the sum of the these arguments.

function sum(a, b, c) {
  return a + b + c;
}

// different arguments to test the code
sum(2, 5, 7);
sum(12, 34, 44);
sum(8, 9, 10);

// console to test
console.log(sum(6, 7, 9));
