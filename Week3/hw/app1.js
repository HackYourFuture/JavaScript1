// 1. Create a function that takes 3 arguments and returns the sum of the these arguments.

function sum(a, b, c) {
  return a + b + c;
}
console.log('sum of the above arguments: ' + sum(100, 99, 1));
