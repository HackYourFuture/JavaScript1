/*
Create an empty object.

Create an object that contains the teachers that you have had so far for the different modules.

Add a property to the object you just created that contains the languages that they have taught you.

*/

console.log(`------- 12 / 13 / 14 -------`)

const emptyObj = new Object();

const hyfModules = new Object();

hyfModules.teachers = [`Philip`, `Rob`, `Unmesh`, `Bonan`, `Yash`];
console.log(`The teachers so far: ${hyfModules.teachers}.`);

hyfModules.languages = [`HTML`, `CSS`, `CLI`, `GIT`, `JS1`];
console.log(`The subjects so far: ${hyfModules.languages}.`);
console.log(`\n`);

