// 3. Create an object and a function that takes the object as a parameter and prints out all of its properties and values.

const person = {
  firstName: 'Happy',
  lastName: 'Man',
  age: 25,
  country: 'Dream Island',
  city: 'Heaven',
};

function personResume(person) {
  console.log(person);
}
personResume(person);
