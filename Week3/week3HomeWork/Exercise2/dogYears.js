function calculateDogAge(age) {
    let puppyAge = age * 7;
    const massege = `Your doggie is ${puppyAge} years old in dog years!`;
    console.log(massege);
}

calculateDogAge(10);
calculateDogAge(4);
calculateDogAge(7);